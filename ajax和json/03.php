<?php
     header('content-type:text/html;charset="utf-8"');

   // $cars=array("大众","奥迪","宝马");
    //var_dump($cars);

    echo '<h1>helloworld</h1>';
  /*
   php声明变量通过$符号进行声明，
   php是弱引用类型:你给变量赋值为什么数据，就是什么数据类型；

   php字符串拼接的时候用的不是加号，而是 .
   php在进行字符串拼接的时候：占位符的方式进行拼接。{变量/表达式}  
  

   
  */

   /*
   数组：索引数组：下标是数字叫做索引数组
        关联数组：下标是字符串的叫关联数组  (map类型)
        全局数组：
         $_GET 接收通过get提交过来的所有数据
         $_POST 接受post提交过来的所有数据
         
         索引数组和关联数组可以结合多为数组
         数组的长度 count($cars) 返回数组的长度 
   */
   


?>